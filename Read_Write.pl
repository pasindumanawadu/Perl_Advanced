# !/usr/bin/perl 

use strict;
use Socket;
use Try::Tiny;

#read from the file
sub read {
	my $file = "input.csv";
	open(F,$ile) || die ("Could not open $file!");
}

sub write {
	open(my $fh, '>>', $filename) or die "Could not open file'$filename'$!";
	print $th "$field!\n";
}

